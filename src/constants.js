const initialStyleState = {
    textAlign: 'left',
    fontWeight: 'normal',
    textDecoration: 'none',
    fontStyle: 'normal',
}

const defaultTitle = 'Новая таблица'

export {initialStyleState, defaultTitle}